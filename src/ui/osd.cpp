#include "ui/osd.h"

#include <cstring>
#include <cstdio>
#include "core/logger.h"

static const char* kCharset = "0123456789:.% xABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";

static const unsigned char kFont[][7] = {
    {0x0E, 0x11, 0x13, 0x15, 0x19, 0x11, 0x0E},  // 0
    {0x04, 0x0C, 0x04, 0x04, 0x04, 0x04, 0x0E},  // 1
    {0x0E, 0x11, 0x01, 0x02, 0x04, 0x08, 0x1F},  // 2
    {0x1F, 0x02, 0x04, 0x02, 0x01, 0x11, 0x0E},  // 3
    {0x02, 0x06, 0x0A, 0x12, 0x1F, 0x02, 0x02},  // 4
    {0x1F, 0x10, 0x1E, 0x01, 0x01, 0x11, 0x0E},  // 5
    {0x06, 0x08, 0x10, 0x1E, 0x11, 0x11, 0x0E},  // 6
    {0x1F, 0x01, 0x02, 0x04, 0x08, 0x08, 0x08},  // 7
    {0x0E, 0x11, 0x11, 0x0E, 0x11, 0x11, 0x0E},  // 8
    {0x0E, 0x11, 0x11, 0x0F, 0x01, 0x02, 0x0C},  // 9
    {0x00, 0x0C, 0x0C, 0x00, 0x0C, 0x0C, 0x00},  // :
    {0x00, 0x00, 0x00, 0x00, 0x00, 0x0C, 0x0C},  // .
    {0x11, 0x12, 0x04, 0x08, 0x10, 0x11, 0x12},  // %
    {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00},  // (space)
    {0x00, 0x00, 0x00, 0x1F, 0x00, 0x00, 0x00},  // x
    {0x0E, 0x11, 0x11, 0x1F, 0x11, 0x11, 0x11},  // A
    {0x1F, 0x11, 0x11, 0x1F, 0x11, 0x11, 0x1F},  // B
    {0x0E, 0x11, 0x10, 0x10, 0x10, 0x11, 0x0E},  // C
    {0x1E, 0x11, 0x11, 0x11, 0x11, 0x11, 0x1E},  // D
    {0x1F, 0x10, 0x10, 0x1E, 0x10, 0x10, 0x1F},  // E
    {0x1F, 0x10, 0x10, 0x1E, 0x10, 0x10, 0x10},  // F
    {0x0E, 0x11, 0x10, 0x17, 0x11, 0x11, 0x0F},  // G
    {0x11, 0x11, 0x11, 0x1F, 0x11, 0x11, 0x11},  // H
    {0x0E, 0x04, 0x04, 0x04, 0x04, 0x04, 0x0E},  // I
    {0x07, 0x02, 0x02, 0x02, 0x02, 0x12, 0x0C},  // J
    {0x11, 0x12, 0x14, 0x18, 0x14, 0x12, 0x11},  // K
    {0x10, 0x10, 0x10, 0x10, 0x10, 0x10, 0x1F},  // L
    {0x11, 0x1B, 0x15, 0x15, 0x11, 0x11, 0x11},  // M
    {0x11, 0x19, 0x15, 0x13, 0x11, 0x11, 0x11},  // N
    {0x0E, 0x11, 0x11, 0x11, 0x11, 0x11, 0x0E},  // O
    {0x1F, 0x11, 0x11, 0x1F, 0x10, 0x10, 0x10},  // P
    {0x0E, 0x11, 0x11, 0x11, 0x15, 0x12, 0x0D},  // Q
    {0x1F, 0x11, 0x11, 0x1F, 0x14, 0x12, 0x11},  // R
    {0x0E, 0x11, 0x10, 0x0E, 0x01, 0x11, 0x0E},  // S
    {0x1F, 0x04, 0x04, 0x04, 0x04, 0x04, 0x04},  // T
    {0x11, 0x11, 0x11, 0x11, 0x11, 0x11, 0x0E},  // U
    {0x11, 0x11, 0x11, 0x11, 0x0A, 0x0A, 0x04},  // V
    {0x11, 0x11, 0x11, 0x15, 0x15, 0x1B, 0x11},  // W
    {0x11, 0x11, 0x0A, 0x04, 0x0A, 0x11, 0x11},  // X
    {0x11, 0x11, 0x0A, 0x04, 0x04, 0x04, 0x04},  // Y
    {0x1F, 0x01, 0x02, 0x04, 0x08, 0x10, 0x1F},  // Z
    {0x00, 0x00, 0x0E, 0x01, 0x0F, 0x11, 0x0F},  // a
    {0x10, 0x10, 0x16, 0x19, 0x11, 0x19, 0x16},  // b
    {0x00, 0x00, 0x0E, 0x10, 0x10, 0x10, 0x0E},  // c
    {0x01, 0x01, 0x0D, 0x13, 0x11, 0x13, 0x0D},  // d
    {0x00, 0x00, 0x0E, 0x11, 0x1F, 0x10, 0x0E},  // e
    {0x06, 0x09, 0x08, 0x1C, 0x08, 0x08, 0x08},  // f
    {0x00, 0x0D, 0x13, 0x13, 0x0D, 0x01, 0x0E},  // g
    {0x10, 0x10, 0x16, 0x19, 0x11, 0x11, 0x11},  // h
    {0x04, 0x00, 0x0C, 0x04, 0x04, 0x04, 0x0E},  // i
    {0x02, 0x00, 0x06, 0x02, 0x02, 0x12, 0x0C},  // j
    {0x10, 0x10, 0x12, 0x14, 0x18, 0x14, 0x12},  // k
    {0x0C, 0x04, 0x04, 0x04, 0x04, 0x04, 0x0E},  // l
    {0x00, 0x00, 0x16, 0x15, 0x15, 0x15, 0x15},  // m
    {0x00, 0x00, 0x16, 0x19, 0x11, 0x11, 0x11},  // n
    {0x00, 0x00, 0x0E, 0x11, 0x11, 0x11, 0x0E},  // o
    {0x00, 0x00, 0x16, 0x19, 0x19, 0x16, 0x10},  // p
    {0x00, 0x00, 0x0D, 0x13, 0x13, 0x0D, 0x01},  // q
    {0x00, 0x00, 0x16, 0x19, 0x10, 0x10, 0x10},  // r
    {0x00, 0x00, 0x0E, 0x10, 0x0E, 0x01, 0x1E},  // s
    {0x08, 0x08, 0x1C, 0x08, 0x08, 0x09, 0x06},  // t
    {0x00, 0x00, 0x11, 0x11, 0x11, 0x13, 0x0D},  // u
    {0x00, 0x00, 0x11, 0x11, 0x11, 0x0A, 0x04},  // v
    {0x00, 0x00, 0x11, 0x11, 0x15, 0x15, 0x0A},  // w
    {0x00, 0x00, 0x11, 0x0A, 0x04, 0x0A, 0x11},  // x
    {0x00, 0x00, 0x11, 0x0A, 0x04, 0x04, 0x04},  // y
    {0x00, 0x00, 0x1F, 0x02, 0x04, 0x08, 0x1F},  // z
};

OSD::~OSD() {
    for (SDL_Texture* t : glyphs_) SDL_DestroyTexture(t);
    glyphs_.clear();
}

void OSD::init(SDL_Renderer* renderer) {
    r_ = renderer;
    const int scale = 2, gw = 5, gh = 7;
    for (int i = 0; i < (int)strlen(kCharset); ++i) {
        SDL_Surface* surf = SDL_CreateRGBSurfaceWithFormat(
            0, gw * scale, gh * scale, 32, SDL_PIXELFORMAT_RGBA32);
        if (!surf) continue;
        SDL_LockSurface(surf);
        Uint32* px = (Uint32*)surf->pixels;
        for (int row = 0; row < gh; ++row) {
            for (int col = 0; col < gw; ++col) {
                if (kFont[i][row] & (0x10 >> col)) {
                    for (int dy = 0; dy < scale; ++dy)
                        for (int dx = 0; dx < scale; ++dx)
                            px[(row * scale + dy) * surf->w + col * scale + dx] = 0xFFFFFFFF;
                }
            }
        }
        SDL_UnlockSurface(surf);
        SDL_Texture* tex = SDL_CreateTextureFromSurface(r_, surf);
        SDL_FreeSurface(surf);
        glyphs_.push_back(tex);
    }
}

SDL_Texture* OSD::glyphFor(char c) {
    const char* p = strchr(kCharset, c);
    if (!p) return nullptr;
    size_t idx = (size_t)(p - kCharset);
    return idx < glyphs_.size() ? glyphs_[idx] : nullptr;
}

void OSD::drawText(int x, int y, const std::string& text, int scale) {
    for (char c : text) {
        SDL_Texture* tex = glyphFor(c);
        if (!tex) continue;
        SDL_Rect dst{ x, y, 5 * scale, 7 * scale };
        SDL_RenderCopy(r_, tex, nullptr, &dst);
        x += 5 * scale + 1;
    }
}

void OSD::toggleInfo() {
    infoVisible_ = !infoVisible_;
    infoToggleTime_ = SDL_GetTicks();
}

void OSD::drawInfoOverlay(int winW, int winH,
                           int vW, int vH, int vBitrate, float vFps, const char* vCodec,
                           int aRate, int aBitrate, const char* aCodec,
                           bool hw, double dur) {
    if (!infoVisible_) return;

    const int sc = 2;
    const int lineH = 7 * sc + 4;
    const int padX = 20, padY = 16;
    char buf[128];
    int x = padX, y = padY;

    auto drawLine = [&](const std::string& s) {
        drawText(x, y, s, sc);
        y += lineH;
    };

    if (vW > 0 && vH > 0) {
        std::snprintf(buf, sizeof(buf), "%dx%d", vW, vH);
        drawLine(std::string("Resolution ") + buf);
    }
    if (vFps > 0) {
        std::snprintf(buf, sizeof(buf), "%.2f", vFps);
        drawLine(std::string("Fps ") + buf);
    }
    if (vCodec[0]) {
        drawLine(std::string("Video ") + vCodec);
    }
    if (vBitrate > 0) {
        std::snprintf(buf, sizeof(buf), "%d", vBitrate);
        drawLine(std::string("VBitrate ") + buf + "kbps");
    }
    if (aCodec[0]) {
        drawLine(std::string("Audio ") + aCodec);
    }
    if (aRate > 0) {
        std::snprintf(buf, sizeof(buf), "%d", aRate);
        drawLine(std::string("SampleRate ") + buf + "Hz");
    }
    if (aBitrate > 0) {
        std::snprintf(buf, sizeof(buf), "%d", aBitrate);
        drawLine(std::string("ABitrate ") + buf + "kbps");
    }
    drawLine(std::string("Decode ") + (hw ? "HW" : "SW"));
    if (dur > 0) {
        int total = (int)dur;
        int h = total / 3600, m = (total % 3600) / 60, s = total % 60;
        if (h > 0)
            std::snprintf(buf, sizeof(buf), "%d:%02d:%02d", h, m, s);
        else
            std::snprintf(buf, sizeof(buf), "%d:%02d", m, s);
        drawLine(std::string("Duration ") + buf);
    }
}

void OSD::draw(const std::string& timeText, double pos, double dur, float vol,
               bool volVisible, bool paused, bool hasMedia) {
    if (!r_) return;
    int w = 0, h = 0;
    SDL_GetRendererOutputSize(r_, &w, &h);
    SDL_SetRenderDrawBlendMode(r_, SDL_BLENDMODE_BLEND);

    if (hasMedia && dur > 0) {
        const int margin = 14, barH = 4;
        int y = h - barH - 8;
        SDL_SetRenderDrawColor(r_, 0, 0, 0, 140);
        SDL_Rect bg{ margin, y, w - margin * 2, barH };
        SDL_RenderFillRect(r_, &bg);
        double ratio = pos / dur;
        if (ratio < 0.0) ratio = 0.0;
        if (ratio > 1.0) ratio = 1.0;
        SDL_SetRenderDrawColor(r_, 46, 204, 113, 255);
        SDL_Rect fg{ margin, y, (int)((w - margin * 2) * ratio), barH };
        SDL_RenderFillRect(r_, &fg);
    }

    if (hasMedia && !timeText.empty()) {
        int tw = (int)timeText.size() * (5 * 2 + 1) - 1;
        drawText(w - tw - 14, h - 8 - 4 - 7 * 2 - 10, timeText, 2);
    }

    if (volVisible) {
        std::string vtext = "音量 ";
        vtext += std::to_string((int)(vol * 100.0f + 0.5f));
        vtext += '%';
        int tw = (int)vtext.size() * (5 * 2 + 1) - 1;
        drawText(w / 2 - tw / 2, h / 2 - 48, vtext, 2);
        SDL_SetRenderDrawColor(r_, 255, 255, 255, 90);
        SDL_Rect barBg{ w / 2 - 60, h / 2 - 22, 120, 6 };
        SDL_RenderFillRect(r_, &barBg);
        SDL_SetRenderDrawColor(r_, 46, 204, 113, 255);
        int bw = (int)(120.0f * vol);
        if (bw > 0) {
            SDL_Rect barFg{ w / 2 - 60, h / 2 - 22, bw, 6 };
            SDL_RenderFillRect(r_, &barFg);
        }
    }

    if (paused && hasMedia) {
        SDL_SetRenderDrawColor(r_, 255, 255, 255, 220);
        SDL_Rect b1{ w / 2 - 18, h / 2 - 16, 12, 32 };
        SDL_RenderFillRect(r_, &b1);
        SDL_Rect b2{ w / 2 + 6, h / 2 - 16, 12, 32 };
        SDL_RenderFillRect(r_, &b2);
    }
}
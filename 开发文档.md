# VPlayer 开发文档

> 本文档为项目唯一权威文档。改动代码后涉及架构/依赖的，必须同步更新本文档。

## 1. 项目概述

个人自用的功能齐全、轻量级 Windows 视频播放器。目标是单文件二进制、启动快、
内存占用低，同时具备主流播放器的大部分功能。

## 2. 开发环境（强制规则）

### 2.1 工具链
- **后端**：Go 1.24.5（`go build` / `go test`），只用标准库（net/http、embed），零第三方模块。
- **前端**：TypeScript + 原生 CSS，编译用 `npx tsc -p web/tsconfig.json`（依赖仅 typescript 一个包，置于 `web/node_modules`）。
- **禁止**使用任何 Python 运行时（含 `E:\anaconda3\python.exe`）运行本项目代码；项目内无 Python 代码。
- **磁盘约束**：C 盘仅剩约 2GB。严禁向 C 盘安装任何东西；Go 模块缓存（GOPATH/GOCACHE）若需重定向，目标为 E: 盘。
- **ffmpeg.exe**（仅作 probe 元数据，只读调用，绝不参与播放）：
  `C:\Users\31697\AppData\Roaming\Python\Python311\site-packages\imageio_ffmpeg\binaries\ffmpeg-win64-v4.2.2.exe`
- **Edge**（唯一播放内核）：`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`

### 2.2 常用命令
```powershell
go build -o vplayer.exe ./server   # 构建后端
npx tsc -p web/tsconfig.json       # 编译前端
.\vplayer.exe                      # 运行（自动拉起 Edge）
```

### 2.3 冒烟测试
- 样本：`C:\Users\31697\AppData\Local\Temp\opencode\s_30s.mp4`（H.264，30 秒）
- 校验点：Edge 窗口出现、视频起播、进度推进、暂停/seek 生效。

## 3. 总体架构

```
┌─ 播放层 (Edge --app= 窗口) ──────────────────────┐
│  无地址栏浏览器窗口 · HTML5 <video> · 自带编解码  │
├─ 前端 (TypeScript + 原生 CSS，go:embed 打包) ────┤
│  控制条 · 快捷键 · 拖拽播放 · 播放列表 · 断点续播 │
├─ 后端 (Go 标准库单二进制) ───────────────────────┤
│  本地 HTTP 服务(127.0.0.1) · 媒体 Range 直读     │
│  历史/播放列表/配置持久化 · ffmpeg 元数据解析     │
│  令牌校验(防局域网访问) · 拉起 Edge               │
└─────────────────────────────────────────────────┘
```

### 3.1 分层职责
- **后端**：只负责文件访问、媒体流（Range/206）、持久化、元数据 probe；不渲染。
- **前端**：全部 UI 与交互；`<video>` 元素负责解码渲染（Edge 自带 H.264/AAC/HEVC）。
- **通信**：REST JSON + 媒体流，全部走 `127.0.0.1:<随机端口>`，URL 带随机令牌。

## 4. 功能规划

### P0（首版 MVP）
- [ ] 播放/暂停/停止、进度条拖动、音量（静音）
- [ ] 本地文件打开（打开对话框 + 命令行参数 + 拖拽文件播放）
- [ ] 倍速（0.25x–4x）、上一集/下一集
- [ ] 全屏、深色主题、基础快捷键
- [ ] 历史记录 + 自动断点续播（>30s 才提示，ask/auto/never）
- [ ] 播放列表（单文件模式起步）
- [ ] 视频信息显示（ffmpeg probe：分辨率/编码/码率/时长）

### P1（增强）
- [ ] 进度条悬停缩略图（ffmpeg 抽帧，HTTP 图片端点）
- [ ] 帧级截图（快捷键一键保存）
- [ ] A-B 循环、逐帧步进
- [ ] 迷你模式 / 画中画（浏览器 API）
- [ ] 字幕文件加载（srt/ass，`<track>` + 简单解析）
- [ ] 无边框窗口（--app 原生能力内最大化）

### P2（进阶）
- [ ] 媒体库：扫描文件夹、分类浏览
- [ ] 在线字幕搜索（外部接口）
- [ ] 音轨切换（多音轨文件需后端转封装能力，待评估）

## 5. 目录结构规划

```
vedioplayer/
├── server/              # Go 后端
│   ├── main.go          # 入口：启动 HTTP + 拉起 Edge
│   ├── api.go           # API 路由与处理器
│   ├── media.go         # Range 媒体流
│   ├── storage.go       # 历史/播放列表/配置 (JSON, os.UserConfigDir)
│   ├── probe.go         # ffmpeg -i 元数据解析（子进程）
│   ├── dist/            # 前端编译产物（go:embed 打包）
│   └── embed.go         # go:embed 声明
├── web/                 # TS 前端
│   ├── src/
│   │   ├── app.ts       # 播放器逻辑（控制/快捷键/拖拽/续播）
│   │   └── ui.ts        # 控制条/播放列表渲染
│   ├── style.css        # 深色主题
│   ├── index.html       # 播放器页面
│   └── tsconfig.json
├── AGENTS.md
├── README.md
└── 开发文档.md
```

## 6. 技术决策记录

| 决策 | 理由 |
|---|---|
| 播放内核用系统 Edge 而非 QtMultimedia | 本机 QtMultimedia(WMF) 视频管线原生崩溃 0xC0000409（MP3 可播、视频文件秒 EndOfMedia+崩溃），查无根因 |
| 播放内核用 Edge 而非 QtWebEngine | conda 构建的 QtWebEngine 无专有编解码器（H.264 mp4 报 MEDIA_ERR_SRC_NOT_SUPPORTED，VP9 WebM 可播）；Edge 为微软官方构建，自带 H.264/AAC/HEVC |
| 后端用 Go 而非 Python/PyQt | 放弃 Python 后零下载可用（Go 1.24.5 已装）；单二进制内嵌前端；零第三方模块 |
| 前端用 TS 而非框架 | 无重型依赖（磁盘受限）；原生 DOM 足够播放器规模 |
| 媒体直读而非转码 | Edge 可原生解码主流格式，避免 ffmpeg 转码的 CPU 开销与起播延迟 |
| ffmpeg 仅作 probe | 与播放完全解耦；转码能力保留备用（万一遇到 Edge 不支持的封装） |
| 本地 HTTP + 令牌 | 浏览器无法直读本地文件，需 Range 服务；令牌防止局域网内其他设备访问 |

### 已知限制
- Edge 不支持的编码（极少见，如部分 DTS 音轨、MJPEG AVI）需后端转封装/转码兜底（P2）。
- 浏览器自动播放策略：必须用户手势或设置 muted 后起播（前端处理）。
- 无原生窗口边框/标题栏（--app 模式由 Edge 提供基础窗体）。

## 7. 数据存储

- 配置/历史/播放列表：`os.UserConfigDir()/VPlayer/`（即 `%APPDATA%\VPlayer\`），JSON 文件。
- 历史条目：`{path, title, position_ms, duration_ms, last_played}`；续播阈值 30s。
- 播放列表：`{path, title}` 数组，启动时默认恢复上次播放项。

## 8. 快捷键约定

| 按键 | 功能 |
|---|---|
| Space | 播放/暂停 |
| ← / → | 快退/快进 5s |
| ↑ / ↓ | 音量 ±10% |
| M | 静音切换 |
| [ / ] | 倍速 -0.25 / +0.25 |
| N / P | 下一集 / 上一集 |
| F / Esc | 全屏切换 / 退出全屏 |
| Ctrl+O / Ctrl+L | 打开文件 / 播放列表 |
| Ctrl+Q | 退出 |

## 9. 约定
- UI 文案用中文。
- 所有持久化读写走后端存储模块，禁止散落在页面代码里。
- 代码改动涉及架构/依赖时，同步更新本文档。
- 工作区内容曾被外部因素清空：**每次完成功能必须 git add -A + commit**（见 AGENTS.md）。